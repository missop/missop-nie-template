nie.config.copyRight.setWhite();
nie.define(function () {
    var PopDialog = nie.require("nie.util.PopDialog"),
        Page = {
            init: function () {
                this.bind();
            },
            bind: function () {

            }
        };
    Page.init();
});
